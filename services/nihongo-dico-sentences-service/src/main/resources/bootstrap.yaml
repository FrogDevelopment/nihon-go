micronaut:
  application:
    name: nihongo-dico-sentences-service

  config-client:
    enabled: true

spring:
  cloud:
    config:
      enabled: true
      uri: http://${docker-secrets.frog_admin_user}:${docker-secrets.frog_admin_password}@support-config-server:8080
