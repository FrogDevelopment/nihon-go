micronaut:
  application:
    name: nihongo-dico-sentences-service

  config-client:
    enabled: true

spring:
  cloud:
    config:
      enabled: true
      uri: http://${FROG_ADMIN_USER}:${FROG_ADMIN_PASSWORD}@support-config-server:8080

