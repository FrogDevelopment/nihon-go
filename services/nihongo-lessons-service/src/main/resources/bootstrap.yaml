micronaut:
  application:
    name: nihongo-lessons-service

  config-client:
    enabled: true

spring:
  cloud:
    config:
      enabled: true
      uri: http://${docker-secrets.frog_admin_user}:${docker-secrets.frog_admin_password}@support-config-server:8080
