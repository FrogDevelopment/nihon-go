spring:
  datasource:
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    url: jdbc:tc:postgresql:11-alpine://localhost/${frog.database.name}

security:
  jwt:
    token:
      signing-key: xxx
